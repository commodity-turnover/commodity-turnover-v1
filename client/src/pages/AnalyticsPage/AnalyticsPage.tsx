import styles from "./analyticsPage.module.scss"

const AnalyticsPage = () => {
  return (
    <div className={`main-content ${styles.analyticsPage}`}>AnalyticsPage</div>
  )
}

export default AnalyticsPage